spring:
  application:
    name: store
  datasource:
    url: jdbc:mysql://localhost:3306/store?createDatabaseIfNotExist=true
    username: ${SPRING_STORE_DATASOURCE_USERNAME}
    password: ${SPRING_STORE_DATASOURCE_PASSWORD}
  jpa:
    show-sql: true
  devtools:
    restart:
      enabled: false
